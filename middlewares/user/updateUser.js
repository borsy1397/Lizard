/**
 * Update a user if we have the data for it
 * Update if we have a res.tpl.user
 */

module.exports = function (objectrepository) {

    return function (req, res, next) {

        return next();
    };

};