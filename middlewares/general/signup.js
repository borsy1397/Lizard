/**
 * Check if the email address or username is already registered, if not
 * create the user
 */

module.exports = function (objectrepository) {

    return function (req, res, next) {

        return next();
    };

};